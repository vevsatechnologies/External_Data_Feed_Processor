// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// PoloniexHistoricDatum is an object representing the database table.
type PoloniexHistoricDatum struct {
	ID            int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	Globaltradeid string    `boil:"globaltradeid" json:"globaltradeid" toml:"globaltradeid" yaml:"globaltradeid"`
	Tradeid       string    `boil:"tradeid" json:"tradeid" toml:"tradeid" yaml:"tradeid"`
	Date          time.Time `boil:"date" json:"date" toml:"date" yaml:"date"`
	Type          string    `boil:"type" json:"type" toml:"type" yaml:"type"`
	Rate          string    `boil:"rate" json:"rate" toml:"rate" yaml:"rate"`
	Amount        string    `boil:"amount" json:"amount" toml:"amount" yaml:"amount"`
	Total         string    `boil:"total" json:"total" toml:"total" yaml:"total"`

	R *poloniexHistoricDatumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L poloniexHistoricDatumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PoloniexHistoricDatumColumns = struct {
	ID            string
	Globaltradeid string
	Tradeid       string
	Date          string
	Type          string
	Rate          string
	Amount        string
	Total         string
}{
	ID:            "id",
	Globaltradeid: "globaltradeid",
	Tradeid:       "tradeid",
	Date:          "date",
	Type:          "type",
	Rate:          "rate",
	Amount:        "amount",
	Total:         "total",
}

// poloniexHistoricDatumR is where relationships are stored.
type poloniexHistoricDatumR struct {
}

// poloniexHistoricDatumL is where Load methods for each relationship are stored.
type poloniexHistoricDatumL struct{}

var (
	poloniexHistoricDatumColumns               = []string{"id", "globaltradeid", "tradeid", "date", "type", "rate", "amount", "total"}
	poloniexHistoricDatumColumnsWithoutDefault = []string{"globaltradeid", "tradeid", "date", "type", "rate", "amount", "total"}
	poloniexHistoricDatumColumnsWithDefault    = []string{"id"}
	poloniexHistoricDatumPrimaryKeyColumns     = []string{"id"}
)

type (
	// PoloniexHistoricDatumSlice is an alias for a slice of pointers to PoloniexHistoricDatum.
	// This should generally be used opposed to []PoloniexHistoricDatum.
	PoloniexHistoricDatumSlice []*PoloniexHistoricDatum
	// PoloniexHistoricDatumHook is the signature for custom PoloniexHistoricDatum hook methods
	PoloniexHistoricDatumHook func(boil.Executor, *PoloniexHistoricDatum) error

	poloniexHistoricDatumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	poloniexHistoricDatumType                 = reflect.TypeOf(&PoloniexHistoricDatum{})
	poloniexHistoricDatumMapping              = queries.MakeStructMapping(poloniexHistoricDatumType)
	poloniexHistoricDatumPrimaryKeyMapping, _ = queries.BindMapping(poloniexHistoricDatumType, poloniexHistoricDatumMapping, poloniexHistoricDatumPrimaryKeyColumns)
	poloniexHistoricDatumInsertCacheMut       sync.RWMutex
	poloniexHistoricDatumInsertCache          = make(map[string]insertCache)
	poloniexHistoricDatumUpdateCacheMut       sync.RWMutex
	poloniexHistoricDatumUpdateCache          = make(map[string]updateCache)
	poloniexHistoricDatumUpsertCacheMut       sync.RWMutex
	poloniexHistoricDatumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var poloniexHistoricDatumBeforeInsertHooks []PoloniexHistoricDatumHook
var poloniexHistoricDatumBeforeUpdateHooks []PoloniexHistoricDatumHook
var poloniexHistoricDatumBeforeDeleteHooks []PoloniexHistoricDatumHook
var poloniexHistoricDatumBeforeUpsertHooks []PoloniexHistoricDatumHook

var poloniexHistoricDatumAfterInsertHooks []PoloniexHistoricDatumHook
var poloniexHistoricDatumAfterSelectHooks []PoloniexHistoricDatumHook
var poloniexHistoricDatumAfterUpdateHooks []PoloniexHistoricDatumHook
var poloniexHistoricDatumAfterDeleteHooks []PoloniexHistoricDatumHook
var poloniexHistoricDatumAfterUpsertHooks []PoloniexHistoricDatumHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PoloniexHistoricDatum) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range poloniexHistoricDatumBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PoloniexHistoricDatum) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range poloniexHistoricDatumBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PoloniexHistoricDatum) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range poloniexHistoricDatumBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PoloniexHistoricDatum) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range poloniexHistoricDatumBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PoloniexHistoricDatum) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range poloniexHistoricDatumAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PoloniexHistoricDatum) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range poloniexHistoricDatumAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PoloniexHistoricDatum) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range poloniexHistoricDatumAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PoloniexHistoricDatum) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range poloniexHistoricDatumAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PoloniexHistoricDatum) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range poloniexHistoricDatumAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPoloniexHistoricDatumHook registers your hook function for all future operations.
func AddPoloniexHistoricDatumHook(hookPoint boil.HookPoint, poloniexHistoricDatumHook PoloniexHistoricDatumHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		poloniexHistoricDatumBeforeInsertHooks = append(poloniexHistoricDatumBeforeInsertHooks, poloniexHistoricDatumHook)
	case boil.BeforeUpdateHook:
		poloniexHistoricDatumBeforeUpdateHooks = append(poloniexHistoricDatumBeforeUpdateHooks, poloniexHistoricDatumHook)
	case boil.BeforeDeleteHook:
		poloniexHistoricDatumBeforeDeleteHooks = append(poloniexHistoricDatumBeforeDeleteHooks, poloniexHistoricDatumHook)
	case boil.BeforeUpsertHook:
		poloniexHistoricDatumBeforeUpsertHooks = append(poloniexHistoricDatumBeforeUpsertHooks, poloniexHistoricDatumHook)
	case boil.AfterInsertHook:
		poloniexHistoricDatumAfterInsertHooks = append(poloniexHistoricDatumAfterInsertHooks, poloniexHistoricDatumHook)
	case boil.AfterSelectHook:
		poloniexHistoricDatumAfterSelectHooks = append(poloniexHistoricDatumAfterSelectHooks, poloniexHistoricDatumHook)
	case boil.AfterUpdateHook:
		poloniexHistoricDatumAfterUpdateHooks = append(poloniexHistoricDatumAfterUpdateHooks, poloniexHistoricDatumHook)
	case boil.AfterDeleteHook:
		poloniexHistoricDatumAfterDeleteHooks = append(poloniexHistoricDatumAfterDeleteHooks, poloniexHistoricDatumHook)
	case boil.AfterUpsertHook:
		poloniexHistoricDatumAfterUpsertHooks = append(poloniexHistoricDatumAfterUpsertHooks, poloniexHistoricDatumHook)
	}
}

// OneP returns a single poloniexHistoricDatum record from the query, and panics on error.
func (q poloniexHistoricDatumQuery) OneP() *PoloniexHistoricDatum {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single poloniexHistoricDatum record from the query.
func (q poloniexHistoricDatumQuery) One() (*PoloniexHistoricDatum, error) {
	o := &PoloniexHistoricDatum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for poloniex_historic_data")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all PoloniexHistoricDatum records from the query, and panics on error.
func (q poloniexHistoricDatumQuery) AllP() PoloniexHistoricDatumSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all PoloniexHistoricDatum records from the query.
func (q poloniexHistoricDatumQuery) All() (PoloniexHistoricDatumSlice, error) {
	var o []*PoloniexHistoricDatum

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to PoloniexHistoricDatum slice")
	}

	if len(poloniexHistoricDatumAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all PoloniexHistoricDatum records in the query, and panics on error.
func (q poloniexHistoricDatumQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all PoloniexHistoricDatum records in the query.
func (q poloniexHistoricDatumQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count poloniex_historic_data rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q poloniexHistoricDatumQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q poloniexHistoricDatumQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if poloniex_historic_data exists")
	}

	return count > 0, nil
}

// PoloniexHistoricDataG retrieves all records.
func PoloniexHistoricDataG(mods ...qm.QueryMod) poloniexHistoricDatumQuery {
	return PoloniexHistoricData(boil.GetDB(), mods...)
}

// PoloniexHistoricData retrieves all the records using an executor.
func PoloniexHistoricData(exec boil.Executor, mods ...qm.QueryMod) poloniexHistoricDatumQuery {
	mods = append(mods, qm.From("\"poloniex_historic_data\""))
	return poloniexHistoricDatumQuery{NewQuery(exec, mods...)}
}

// FindPoloniexHistoricDatumG retrieves a single record by ID.
func FindPoloniexHistoricDatumG(id int, selectCols ...string) (*PoloniexHistoricDatum, error) {
	return FindPoloniexHistoricDatum(boil.GetDB(), id, selectCols...)
}

// FindPoloniexHistoricDatumGP retrieves a single record by ID, and panics on error.
func FindPoloniexHistoricDatumGP(id int, selectCols ...string) *PoloniexHistoricDatum {
	retobj, err := FindPoloniexHistoricDatum(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindPoloniexHistoricDatum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPoloniexHistoricDatum(exec boil.Executor, id int, selectCols ...string) (*PoloniexHistoricDatum, error) {
	poloniexHistoricDatumObj := &PoloniexHistoricDatum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"poloniex_historic_data\" where \"id\"=$1", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(poloniexHistoricDatumObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from poloniex_historic_data")
	}

	return poloniexHistoricDatumObj, nil
}

// FindPoloniexHistoricDatumP retrieves a single record by ID with an executor, and panics on error.
func FindPoloniexHistoricDatumP(exec boil.Executor, id int, selectCols ...string) *PoloniexHistoricDatum {
	retobj, err := FindPoloniexHistoricDatum(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *PoloniexHistoricDatum) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *PoloniexHistoricDatum) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *PoloniexHistoricDatum) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *PoloniexHistoricDatum) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no poloniex_historic_data provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(poloniexHistoricDatumColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	poloniexHistoricDatumInsertCacheMut.RLock()
	cache, cached := poloniexHistoricDatumInsertCache[key]
	poloniexHistoricDatumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			poloniexHistoricDatumColumns,
			poloniexHistoricDatumColumnsWithDefault,
			poloniexHistoricDatumColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(poloniexHistoricDatumType, poloniexHistoricDatumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(poloniexHistoricDatumType, poloniexHistoricDatumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"poloniex_historic_data\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"poloniex_historic_data\" DEFAULT VALUES"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into poloniex_historic_data")
	}

	if !cached {
		poloniexHistoricDatumInsertCacheMut.Lock()
		poloniexHistoricDatumInsertCache[key] = cache
		poloniexHistoricDatumInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single PoloniexHistoricDatum record. See Update for
// whitelist behavior description.
func (o *PoloniexHistoricDatum) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single PoloniexHistoricDatum record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *PoloniexHistoricDatum) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the PoloniexHistoricDatum, and panics on error.
// See Update for whitelist behavior description.
func (o *PoloniexHistoricDatum) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the PoloniexHistoricDatum.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *PoloniexHistoricDatum) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	poloniexHistoricDatumUpdateCacheMut.RLock()
	cache, cached := poloniexHistoricDatumUpdateCache[key]
	poloniexHistoricDatumUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			poloniexHistoricDatumColumns,
			poloniexHistoricDatumPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update poloniex_historic_data, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"poloniex_historic_data\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, poloniexHistoricDatumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(poloniexHistoricDatumType, poloniexHistoricDatumMapping, append(wl, poloniexHistoricDatumPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update poloniex_historic_data row")
	}

	if !cached {
		poloniexHistoricDatumUpdateCacheMut.Lock()
		poloniexHistoricDatumUpdateCache[key] = cache
		poloniexHistoricDatumUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q poloniexHistoricDatumQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q poloniexHistoricDatumQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for poloniex_historic_data")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o PoloniexHistoricDatumSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o PoloniexHistoricDatumSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o PoloniexHistoricDatumSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PoloniexHistoricDatumSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), poloniexHistoricDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"poloniex_historic_data\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, poloniexHistoricDatumPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in poloniexHistoricDatum slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *PoloniexHistoricDatum) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *PoloniexHistoricDatum) UpsertGP(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *PoloniexHistoricDatum) UpsertP(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *PoloniexHistoricDatum) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no poloniex_historic_data provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(poloniexHistoricDatumColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()

	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	poloniexHistoricDatumUpsertCacheMut.RLock()
	cache, cached := poloniexHistoricDatumUpsertCache[key]
	poloniexHistoricDatumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			poloniexHistoricDatumColumns,
			poloniexHistoricDatumColumnsWithDefault,
			poloniexHistoricDatumColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			poloniexHistoricDatumColumns,
			poloniexHistoricDatumPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert poloniex_historic_data, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(poloniexHistoricDatumPrimaryKeyColumns))
			copy(conflict, poloniexHistoricDatumPrimaryKeyColumns)
		}
		cache.query = queries.BuildUpsertQueryPostgres(dialect, "\"poloniex_historic_data\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(poloniexHistoricDatumType, poloniexHistoricDatumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(poloniexHistoricDatumType, poloniexHistoricDatumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert poloniex_historic_data")
	}

	if !cached {
		poloniexHistoricDatumUpsertCacheMut.Lock()
		poloniexHistoricDatumUpsertCache[key] = cache
		poloniexHistoricDatumUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single PoloniexHistoricDatum record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *PoloniexHistoricDatum) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single PoloniexHistoricDatum record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *PoloniexHistoricDatum) DeleteG() error {
	if o == nil {
		return errors.New("models: no PoloniexHistoricDatum provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single PoloniexHistoricDatum record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *PoloniexHistoricDatum) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single PoloniexHistoricDatum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PoloniexHistoricDatum) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no PoloniexHistoricDatum provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), poloniexHistoricDatumPrimaryKeyMapping)
	sql := "DELETE FROM \"poloniex_historic_data\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from poloniex_historic_data")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q poloniexHistoricDatumQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q poloniexHistoricDatumQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no poloniexHistoricDatumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from poloniex_historic_data")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o PoloniexHistoricDatumSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o PoloniexHistoricDatumSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no PoloniexHistoricDatum slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o PoloniexHistoricDatumSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PoloniexHistoricDatumSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no PoloniexHistoricDatum slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(poloniexHistoricDatumBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), poloniexHistoricDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"poloniex_historic_data\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, poloniexHistoricDatumPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from poloniexHistoricDatum slice")
	}

	if len(poloniexHistoricDatumAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *PoloniexHistoricDatum) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *PoloniexHistoricDatum) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *PoloniexHistoricDatum) ReloadG() error {
	if o == nil {
		return errors.New("models: no PoloniexHistoricDatum provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PoloniexHistoricDatum) Reload(exec boil.Executor) error {
	ret, err := FindPoloniexHistoricDatum(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *PoloniexHistoricDatumSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *PoloniexHistoricDatumSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PoloniexHistoricDatumSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty PoloniexHistoricDatumSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PoloniexHistoricDatumSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	poloniexHistoricData := PoloniexHistoricDatumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), poloniexHistoricDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"poloniex_historic_data\".* FROM \"poloniex_historic_data\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, poloniexHistoricDatumPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&poloniexHistoricData)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PoloniexHistoricDatumSlice")
	}

	*o = poloniexHistoricData

	return nil
}

// PoloniexHistoricDatumExists checks if the PoloniexHistoricDatum row exists.
func PoloniexHistoricDatumExists(exec boil.Executor, id int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"poloniex_historic_data\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if poloniex_historic_data exists")
	}

	return exists, nil
}

// PoloniexHistoricDatumExistsG checks if the PoloniexHistoricDatum row exists.
func PoloniexHistoricDatumExistsG(id int) (bool, error) {
	return PoloniexHistoricDatumExists(boil.GetDB(), id)
}

// PoloniexHistoricDatumExistsGP checks if the PoloniexHistoricDatum row exists. Panics on error.
func PoloniexHistoricDatumExistsGP(id int) bool {
	e, err := PoloniexHistoricDatumExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// PoloniexHistoricDatumExistsP checks if the PoloniexHistoricDatum row exists. Panics on error.
func PoloniexHistoricDatumExistsP(exec boil.Executor, id int) bool {
	e, err := PoloniexHistoricDatumExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
